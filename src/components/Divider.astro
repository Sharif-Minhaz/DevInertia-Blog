---
const {title} = Astro.props;
---

<div class="flex items-center justify-center">
    <div class="flex-grow border-t border-gray-300 dark:border-gray-800" />
    {title ? <span class="mx-4 text-gray-500">{title}</span> : <slot />}
    <div class="flex-grow border-t border-gray-300 dark:border-gray-800" />
</div>